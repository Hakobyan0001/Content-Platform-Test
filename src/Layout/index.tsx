import { Box } from '@mui/material';
import App from '../App';
import { Header } from '../view/Components';

export default function Layout() {
  return (
    <Box>
      <Header />
      <App />
    </Box>
  );
}
